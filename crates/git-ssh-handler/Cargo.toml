[package]
name = "slasha-git-ssh-handler"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.102"
diesel = { version = "2.2.0", features = ["sqlite", "chrono"] }
dirs = "6.0.0"
models = { path = "../models" }
